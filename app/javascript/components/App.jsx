import React from "react";
import Routes from "../routes";

// export component to render the routes within fragments. This component will render at applications entry
// point making the routes available whenever the application is loaded
export default props => <>{Routes}</>;

